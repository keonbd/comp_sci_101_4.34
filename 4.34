def get_num_of_characters(inputStri):
    count = 0
    for i in inputStri:
        count += 1
    
    #phrase = input('Enter a sentence or phrase:\n')
    #lensphrase = len(inputStri)
    return count
def output_without_whitespaces(inputStri):
    String = inputStri.replace(' ', '')
    return String
if __name__ == '__main__':
    # Type your code here
    phrase = input('Enter a sentence or phrase:\n')
    print()
    print('You entered:', phrase)
    print()
    print('Number of characters:', len(phrase))
    print('String with no whitespace:', output_without_whitespaces(phrase))
